{
  "name": "solid-ui-components",
  "type": "module",
  "version": "0.0.1",
  "packageManager": "pnpm@8.6.12",
  "scripts": {
    "lint": "turbo lint",
    "build": "turbo build",
    "build:registry": "pnpm --filter=docs build:registry",
    "dev": "turbo dev"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.49.0",
    "turbo": "^1.10.13"
  }
}
